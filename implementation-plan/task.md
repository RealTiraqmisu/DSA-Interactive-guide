# about
ไฟล์นี้จะใช้สำหรับเขียน task ที่อยากให้นายทำ โดยฉันจะแยกเป็นหัวข้อ ๆ ให้นายนำหัวข้อนั้นไปตั้งชื่อและสร้างไฟล์ใน folder implementation plan 
เช่น task1 ให้ทำ checklist ตามนี้ ให้นายนำ implementation plan ที่นายสรัางให้ฉันอ่าน ใส่เข้าไปในไฟล์ที่ชื่อ task1.md โดยทำแต่ละ task ในไฟล์นั้นเป็น checkbox

# task-1
[] รู้สึกว่า week 1 อ่านยาก (จริง ๆ ก็ทุุกอันแหละ แต่โฟกัสที่ week1 ก่อน) ให้นายลองศึกษาเว็บที่ฉันแปะเข้าไปที่ `gemini.md` ดู อันนั้นสีสวย ทำให้น่าอ่านมากขึ้น ลองไปส่อง ๆ ดูได้ ใช้เป็นแนวทาง ไม่ได้ให้ทำสีตามนั้น
[] ลองศึกษาดูว่า ทำไมเว็บเดิมถึงอ่านยาก ไม่สบายตา แล้วสีอะไรที่เหมาะสมในการทำเว็บไซต์ ให้นำมาแก้ไขเว็บปัจจุบันนี้ โดยยังคงให้มันเป็น theme เดิม แต่เติมสีใหม่ ๆ เข้ามา เพราะเพื่อนทำไปแล้ว เดี๋ยวจะตกใจว่ามันเปลี่ยนไปมากมาย
[] อยากให้แต่ละสีแบ่งความสำคัญด้วย ไม่ใช่ใช้สีมั่ว ๆ ซั่ว ๆ ไรงี้ 

# task-2
[] หน้าแรก ของไฟล์ รู้สึกว่าการแบ่งช่อง จัดการไฟล์ ยังดูไม่ดี และดูยากมาก ไม่แน่ใจว่า ถ้าทำเป็นให้กล่องมันหุบเข้าไปก่อน เมื่อกดที่กล่องจะเข้าไปที่ interactive guide แต่ละวัน แล้วพอกดเปิดลูกศรที่คล้าย ๆ dropdown ออกมา ให้แสดงเป็นไฟล์ต่าง ๆ ที่เกี่ยวข้องในวันนั้น ๆ น่าจะดีกว่าไหม

# task-3
ฉันจะบอกปัญาที่พบ ให้นายลองคิดดูว่าควรจะแก้ยังไงดี
[x] ปุ่มหุบ navbar ที่เป้นอยู่ตอนนี้กดยาก อยากให้มันคล้าย ๆ notion 
[x] คำว่า dsa course รู้สึกมันไม่จำเป็น ให้ใส่เป็นชื่อ week หรือชื่อ day อะไรงี้จะดีกว่าไหม คิดว่ายังไง แล้วอาจเอาปุ่มหุบ navbar ไปวางด้วย อันนี้แล้วแต่นาย ดูตามความสวยงามได้เลย
[x] ปุ่มกลับ ตรง python programming guide มันเหมือนปุ่มเปลี่ยน module ยังไงไม่รุู้ แก้ไขยังไงดี อยากให้มีปุ่ม back to home (พาไปหน้า index.html) อันนึง แล้วมีปุ่มถัดไป ย้อนกลับ ในแต่ละ day เอา ซึ่งก็คือเลื่อนไปตามเนื้อหาที่มีใน navbar ด้านข้างซ้าย อะไรงี้ ลองดู ๆ ให้หน่อยนะ
[x] แก้ไขเพิ่มเติม
    [x] หุบ left navbar ตอนนี้สวยงามมาก แต่อยากให้หุบแล้วยังเห็น icon ของแต่ละบทอยู่ และสามารถกดได้ โดยที่กดแล้วมันจะไม่กางออกมา นอกจากจะไปกดปุ่มกาง navbar ด้านบนสุด (ปุ่มกาง navbar อาจจะแทนที่หมวกบัณฑิตไปถ้าเกะกะ) และอยากให้มีเส้นแนวตั้งแบ่งอยู่ จะได้รู้ อันไหน nav อันไหนเนื้อหา
    [x] ปุ่ม next module/next section มองไม่ชัดเลย เปลี่ยนสีให้หน่อยได้ไหม
    [x] week1 ชื่อ Introduction & Basic Python ไม่ใช่ python recursion อะไรนั่น แก้ให้ด้วย

# task-4-design-fix
- พวกกล่องโค้ด C Hash Map (Custom) Python List Resizing (Live Editor) และกล่องอื่น ๆ ที่ใช้ design แบบเดียวกัน อยากให้เปลี่ยนเป็น 
    - [x] อยากให้กล่องที่เป็ฯ code สีเหมือนกับพื้นหลัง ide ทั้งตัวของ syntax ที่เหมือนกับตัว ide และพื้นหลังด้วย พอจะเป็นไปได้ไหม ตอนนี้แบบเดิมสีขาว ไม่มีการไฮไลท์เรื่อง sytax คำสั่ง เหมือนใน ide เลย ทำให้มองยากมาก ๆ
    - [x] อาจใช้วิธีห่อกล่อง code ด้วย border สี ๆ หรือจะเอาแบบที่เป็นอยู่ตอนนี้ก็ได้ ลองดูให้หน่อยว่าแบบไหนเหมาะสม และอ่านง่ายกว่ากัน
    - [x] กล่องที่เป็น interactive เกม หรืออะไรประมาณนั้น อยากให้ใช้วิธีว่า ให้ border เป็นสีแบบเดียวกะสี border ซ้ายอย่างเดียวตอนนี้ และตัวสีของกล่องก็คือสีของ border ที่อ่อนลง อาจจะลง transperency หรือ ลดให้สีอ่อนลงก็ได้
- [x] ปุ่ม next section อยากให้มันเป็นเขียวใส ๆ และมีขอบเป็นเขียว พอเอาอะไรไป hover สีเข้มขึ้นนิดนึง 
- [x] ปุ่ม previous เหมือนปุ่ม next section แต่เป็นสีแดง หรือสีเทาแทน
- [x] สี class live-code-textarea ตอนนี้มันขึ้นเหมือนตัวเอป็น terminal มันต้องขึ้นเหมือนตัวเองเป็น python ide ที่ hilight syntax ไว้ 
- [x] just thinking that if python side have run code button, why c side dont have it. is it look unbalance? what do you think? if you think its unbalance you can add run code button to c side too. but if you think its fine dont add it
- [x] บอกตามตรงว่าพวก comparison box ที่มีอยู่เดิม มองไม่ออกเลยว่ามัน compare กันอยู่ โคตรงง นายคิดว่าจะซ่อมมันยังไง
- [x] ชอบก้อนที่เขียนว่า vs มาก ฉะนั้นเพื่อความชัดเจนมากขึ้น ลองเปลี่ยนสี text ด้านบน หรือเปลี่ยนแถบที่เห็นพื้นหลัง text ให้เป็นสี ๆ  อย่าง python สีนึง c สีนึง จะได้เห็นชัด ๆ ว่าเปรียบเทียบอะไรกับอะไร ลองทำดูว่าแบบไหนดีก่ากัน
- [x] อย่าให้ตัว code box มีแถบเลื่อน ๆ โผล่ขึ้นมา ถ้ามันใหญ่ก็ให้มันยืดทั้งสองกล่องเลย
- [x] ตรงไหนที่มีการเปรียบเทียบ ให้ใส่วงกลม versus เข้าไปด้วย และทำสีให้โดดเด่น ตอนนี้มันกลมกลืนมาก
- [x] class mem-side c-mem-side กับอันข้าง ๆ ทำให้มันเป็นสีแดงใส ๆ น้ำเงินใส ๆ แล้วก็เพิ่ม versus เข้าไปด้วย (ตามข้อด้านบน)
- [x] เหนือคำว่า Focus on Logic, rich--build..., readability ... ให้มีเลข 1-3 อยู่ข้างบนด้วย มองแล้วให้รู้ว่านี่คือสรรพคุณของการใช้ python
- [x] เจอว่าตรงไหน text ไม่ชัด ให้แก้ไขได้เลย
- [x] สีของ code-compare-block ทุกกล่อง ทำให้ can see text properly         
    - ถ้าหาไม่เจอ ยกตัวอย่าง เช่นกล่องที่มี class visualizer-code-steps-panel หัวข้อ active code trace สีแบบมองไม่เห็นอะไรเลย
- [x] Why Python for Data Structures & Algorithms? อยากเปลี่ยนเป็นประมาณว่า 3 reason why python อะไรประมาณนั้น เพราะข้างล่างจะมีเลข 1-3 อยู่ เลยอยากให้สอดคล้องกัน
- [x] ปุ่ม run code แก้เป็นสีเขียวที่มองแล้วสวยน่ากด
- [x] ปุ่ม next section/previous section แก้ให้เป็นเขียว/เทา สด ๆ ที่มองแล้วน่ากด รู้ว่าเป็นปุ่ม ไม่ใช่กล่องใส ๆ แบบเดิม

- [x] ตรงโค้ดอันนี้ กล่องยังสีอ่อนอยู่ แก้เป็นกล่องสีเข้มเลยก็ได้ เหมือนกล่องโค้ด ถ้า syntax  color ไม่ขึ้น ให้ทำให้มันขึ้นด้วย แต่นายต้องอ่านก่อนนะว่ามันเป็นกล่องสำหรับโค้ดใช่ไหม ไม่ใช่ output 
    # Initializing collection:
    fruits = ["apple", "banana", "cherry"] 
    หรือพวก class code-container exception-code-panel flow-code-panel editor-textarea visualizer-code-display แก้เป็นกล่องสีเข้มไปเลยก็ได้ ลุยๆ

- [x] background: var(--bg-primary); border: 1px solid var(--border-color); border-radius: 12px; padding: 1rem 1.1rem; margin-bottom: 1.25rem; กล่องที่มี style ยาว ๆ แบบนี้ จริงๆถ้ามันใช้ class ของเดิมที่มีอยู่แล้วได้จะดีมาก แล้วไม่ต้องมาเขียน style ยาว ๆ แบบนี้ แต่ถ้าทำไม่ได้ก็คงไว้แบบเดิม แค่เช็คหน่อยว่า ถ้า text อ่อน กล่องควรเข้ม

- [x] class editor-output console-output-box visualizer-panel trace-console-panel ทำตัวให้มันเหมือน terminal ด้วย เหมือนบางอันยังหลง ๆ ไม่ใช่อยู่ 

- [x] สี syntax ผิด มันไม่มีสีเลย
    # Let's explore Python structures
    point = (3, 4) # Tuple
    names = ["Alice", "Bob", "Charlie"] # List
    grades = {"Alice": "A", "Bob": "B"} # Dictionary

    # Try modifying names list
    names.append("David")
    print("Names:", names)

- [x] เช็คด้วยว่า กล่องไหนสีอ่อน แล้ว text ยังอ่อน นายก็แก้ไปเลย ตามตัวอย่างที่มีอยู่ในไฟล์นั้นและดุลยพินิจของนาย

# task-5-big-improve-design-n-uxui
- [x] ลองอ่านไฟล์ แล้วหาสิ่งที่คิดว่าควรแก้ไข ทั้งเรื่องหน้าตา ux ที่ดีต้องเป็นยังไง เรื่อง ui สี ความชัดเจนของสี text เอาง่าย ๆ คือเรื่อง design ทำโดยละเอียดนะ ไล่ทุกหัวข้อเลย จากนั้นสร้าง implementation-plan ที่ละเอียดมาก ๆ ใส่ไว้ใน /implementation-plan folder แล้วตั้งชื่อไฟล์ตาม heading 1 นี้

# task-6-update-from-instructor-n-content-fixes-1
- [x] ศึกษาเรื่อง การเขียน function พื้นฐาน จากนั้น นำมาเขียนเป็นหัวข้อใหม่ให้หน่อย in `week1.html` ในหน้าของ module 1: python basics เพิ่มมาเป็น dropdown ระหว่าง try except กับ loop & conditional เขียนเป็นภาษาอังกฤษตามที่บอกไว้ใน `gemini.md` โดยจะแปะ source ที่จะให้เรียนไว้ใน bullet ข้างล่างนี้
    - https://www.w3schools.com/python/python_functions.asp
    - https://www.w3schools.com/python/python_arguments.asp

- [x] module 2 recursion เหมือนไม่ได้สอนวิธีเขียน recursion ด้วย function เลย รึเปล่า หรือหาไม่เจอเอง ช่วยเติมเข้าไปหน่อยได้ไหม สอนแค่ว่าเขียนยังไง แต่เอาไปใช้ยังไงยังไม่มี แบบที่เอา function ไปใส่ใน def อีกทีนึงอันนี้ยังไม่เห็น เห็นแต่การ return แบบ recursion หรือฉันเข้าใจผิด

